---
# Collections required by the roles in this repo.
# Install with:
#   ansible-galaxy install -r ansible/requirements.yml
collections:
  - name: community.general # ufw, timezone, sudoers modules
  - name: ansible.posix # authorized_key module
