# Managed by Ansible — role: backup. Do not edit manually.
# Values are injected at runtime from Infisical; file is root-only (0600).
export AWS_ACCESS_KEY_ID="{{ backup_restic_aws_access_key_id }}"
export AWS_SECRET_ACCESS_KEY="{{ backup_restic_aws_secret_access_key }}"
export RESTIC_REPOSITORY="{{ backup_restic_repo }}"
export RESTIC_PASSWORD="{{ backup_restic_password }}"
